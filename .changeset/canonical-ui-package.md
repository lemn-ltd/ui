---
"@lemn-ltd/ui": patch
---

Declare the GitHub Packages identity as `@lemn-ltd/ui` and align workspace,
showcase, documentation, validation, and release references with that canonical
name without changing exports or component behavior.

Keep the public `IconName` declaration compatible with strict React 19.2
consumers while retaining `lucide-react` 0.469.0 at runtime.
