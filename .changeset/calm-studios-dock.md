---
"@lemn-ltd/brand-studio": minor
---

Add controlled wizard-step and visual-mode props, make the Brand Studio step rail horizontally scrollable, and expose the package-owned live preview for host-managed surfaces such as `DockPanel` while preserving inline preview as the default.
